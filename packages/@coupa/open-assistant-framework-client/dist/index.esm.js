export { EventEmitter } from 'eventemitter3';

// OAF Client initialization function
function initOAFInstance(config) {
  return {
    setSize: async options => {
    },
    navigate: async path => {
    },
    openForm: async formConfig => {
    },
    closeForm: async () => {},
    submitForm: async formData => {
    },
    on: (event, callback) => {
    },
    off: (event, callback) => {
    },
    emit: (event, data) => {
    }
  };
}

export { initOAFInstance };
